/**
 * @pulse/design-tokens — the PULSE token source (UI authority).
 *
 * Becomes the owner of `globals.css` (relocated from the repo root) and the
 * machine-readable token exports in T-14 (Session 4), wired through Tailwind v4
 * `@theme` per Design System v1.1. Tokens only — never hardcoded color,
 * spacing, font, radius, shadow, motion, or z-index (constitution §3).
 *
 * Intentionally empty in Session 1 — tokens are wired in T-14.
 */
export {};

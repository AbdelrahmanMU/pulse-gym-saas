/**
 * @pulse/types — shared, dependency-free contracts.
 *
 * The home for cross-cutting type contracts and the platform-adapter /
 * Authentication-Adapter interfaces (`IClock`, `IIdGenerator`, `ICurrentUser`,
 * `AuthenticationAdapter`) introduced in T-19 / T-26 (Session 3). This package
 * stays dependency-free (monorepo-strategy §4) and contains no business logic.
 *
 * Intentionally empty in Session 1 — no contracts exist yet.
 */
export {};

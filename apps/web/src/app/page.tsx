// Placeholder home route (T-06). No business routes, no business logic, no design
// system yet — those land in Sessions 3–4. Its only job is to prove the app boots
// and renders under strict TypeScript + React Server Components.
export default function HomePage() {
  return (
    <main>
      <h1>PULSE</h1>
      <p>Platform foundation is online.</p>
    </main>
  );
}
